# Unhappy CLI Development Environment
#
# This file is for direnv users who want automatic environment switching
# when entering this directory.
#
# Setup:
#   1. Install direnv: https://direnv.net/
#   2. Copy this file: cp .envrc.example .envrc
#   3. Run: direnv allow
#
# The .envrc file is gitignored, so each developer can customize it.

export UNHAPPY_HOME_DIR="$HOME/.unhappy-dev"
export UNHAPPY_VARIANT="dev"
export UNHAPPY_SERVER_URL="${UNHAPPY_SERVER_URL:-https://api.unhappy.im}"

echo "🔧 DEV environment activated (data: $UNHAPPY_HOME_DIR)"
