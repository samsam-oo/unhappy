name: Monorepo TypeScript typecheck

on:
    pull_request:
        paths:
            - 'packages/unhappy-cli/**'
            - 'packages/unhappy-server/**'
            - '.github/workflows/typecheck.yml'
    push:
        branches:
            - main
        paths:
            - 'packages/unhappy-cli/**'
            - 'packages/unhappy-server/**'
            - '.github/workflows/typecheck.yml'

jobs:
    typecheck:
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: 20
                  cache: yarn

            - name: Install dependencies
              run: yarn install --frozen-lockfile

            - name: TypeScript typecheck (CLI)
              run: yarn workspace unhappy-cli typecheck

            - name: TypeScript typecheck (Server)
              run: yarn workspace unhappy-server build
